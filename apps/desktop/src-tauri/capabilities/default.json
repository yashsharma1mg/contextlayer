{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "default",
	"description": "Main window: the splash screen that hands off to the local Studio server.",
	"windows": ["main"],
	"permissions": ["core:default"]
}
